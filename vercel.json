{
  "version": 2,
  "buildCommand": "cd fmeda-frontend && npm install && CI=false npm run build",
  "outputDirectory": "fmeda-frontend/build",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}